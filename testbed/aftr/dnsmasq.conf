# Listen only on the AFTR's IPv6 interface
interface=veth-aftr
bind-interfaces

# Resolve the AFTR's FQDN to its own IPv6 address
address=/aftr.dslite.example.com/2001:a:b:c::1

# Forward other queries (optional)
server=8.8.8.8
